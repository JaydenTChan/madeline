//This is where the background images will change.

switch(global.part){
    case 1:
        switch(global.state){
            case 0:
                draw_sprite(classroomSpr,0,x,y);
                break;                                          
        }
        break;
    case 2:
        break;
}
